function add(num){   //this is the normal funcion 
   return num + num
}

const divide  = (num, num2) => {   //this is array funcion is work in the variable const
    return num / num2
} 
const square = num => num * num; //this is for one paramether and simplific the code and apart no use return because is simple

console.log(square(2));